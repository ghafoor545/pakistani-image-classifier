# Vision Transformer Image Classifier Web App

Deployed version of the refined ViT from `Refined_Training.ipynb`

## How to run

1. Put your files in `model/` folder:
   - `refined_vit_model.pth`
   - `refined_vit_class_info.json`

2. Install dependencies:
```bash
pip install -r requirements.txt
streamlit run app.py
